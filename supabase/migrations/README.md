# Legacy migrations — do not apply

The canonical shared schema is maintained under
`SellerHub/supabase/migrations`. These files are retained only for historical
reference and must not be run against production or a new environment.
